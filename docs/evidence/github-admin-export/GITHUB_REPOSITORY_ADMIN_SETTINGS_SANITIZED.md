# GitHub Repository Admin Settings — Sanitized Export

## Repository

```text
nanotech-solutions-norway/NTSN-Orchestrator-Control-Plane
```

## Export status

```text
PARTIAL_CONNECTOR_VERIFIED_EXPORT
```

## Sanitized metadata

| Setting | Value | Source |
|---|---|---|
| Repository visibility | `public` | GitHub connector metadata |
| Default branch | `main` | GitHub connector metadata |
| Archive status | `false` | GitHub connector metadata |
| Auto-merge allowed | `false` | GitHub connector metadata |
| Merge commit allowed | `true` | GitHub connector metadata |
| Squash merge allowed | `true` | GitHub connector metadata |
| Rebase merge allowed | `true` | GitHub connector metadata |
| Update branch allowed | `false` | GitHub connector metadata |
| Workflow permissions default | `unknown` | Not provided in uploaded export file |

## Upload review

The uploaded file was received, but it still contained placeholder values. Connector-verified repository metadata has been applied where available; unresolved values remain explicitly `unknown`.

## Notes

```text
Sanitized metadata only. No protected values included.
```
